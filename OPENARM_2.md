# OpenArm 2.0 環境

## 出典とモデル

- 公式MJCF: https://github.com/enactic/openarm_mujoco/tree/ce761e2eb1079c3e7cafe515a375930adedad190/v2
- 公式変更点: https://docs.openarm.dev/overview/whats-new-in-2.0/
- モーター: https://docs.openarm.dev/hardware/openarm-2.0/motor/

公式pedestalモデルをMuJoCo 3.6.0で展開し、同じモデルからURDF・MoveIt設定を生成します。
支柱取り付け高さ0.698m、関節軸・可動域・質量・慣性・接触メッシュは公式値です。
制御用 `openarm_{side}_hand` フレームはee_base_linkにRx(pi)の固定変換を追加したもので、物理形状は変えません。

2.0の指は回転関節です。左は正方向、右は負方向へ開きます。
GUI/MoveItのgripperグループの `open` は左+0.65rad、右-0.65rad、`home` は閉じた0radです。
物理側は公式の1モーター・連動指の等式制約を維持します。
アームの最大トルク40/27/7Nm、グリッパー最大7Nmは公式モデル/モーターのピーク値です。
連続定格や温度上昇・電流制御・バックラッシュは再現していません。

## 把持デモ

右/左それぞれ100gの直方体を机上のx=0.29m、y=±0.18mへ配置します。
横向きの手首姿勢で接近し、0.38mの制御フレーム高さで閉じ、0.48mまで持ち上げて3秒保持、再配置してホームへ戻ります。
左右の指はそれぞれ自分の対象物のみ接触許可します。物体から離れた後は通常の衝突判定へ戻します。
MuJoCoの物体は接触力で保持し、固定/溶接や持ち上げ中の位置書き換えは行いません。
成功判定は両物体の持ち上げ9cm以上・3秒中の上下変動5mm以下です。

## カメラと作業環境

1.0で用意したD435相当RGB-D、机、直方体、500mLボトル近似、GUI、GPU描画、ROS 2/MoveIt、VS Codeを共用します。
胸部D435は1.0用公式ブラケットに基づく本プロジェクトの追加設備です。
2.0公式の指定マウント/指定高さと同一であるとの主張はしていません。実機2.0用取り付けは別途確認してください。
公式2.0の手首カメラ位置はMJCFに保持していますが、手首カメラのROS画像配信はこの環境では追加していません。
既存のD435相当画像トピックを使用してください。

## 開発

`oa-code` は選択中のDockerコンテナに接続します。
`src/openarm_demos` はクローンした `docker/demos/openarm_demos` へのリンクです。
起動時に `colcon build --symlink-install` され、Pythonコードの修正は同じソースから実行されます。
コア環境は `environment/docker/app` を編集し `bash start.sh --robot-version 2` でイメージへ反映します。
機種切り替え後はVS Codeを閉じて `oa-code` で新しいコンテナへ接続してください。

## 検証結果

RTX 3060 Ti / Ubuntu 24.04で確認。
- 1.0の両腕: 左右とも持ち上げ9.7cm、3秒の変動0.63mm、再配置・ホーム復帰まで成功。
- 2.0の両腕: 左右とも持ち上げ9.7cm、3秒の変動約0.30mm、再配置・ホーム復帰まで成功。
- 左右の接触許可/反対側への不許可を実際のPlanningSceneから検証。
- D435相当カラー320×180と深度424×240について複数フレームの時刻更新を確認。
- オフラインの対話選択、不正入力後の再入力、NVIDIA描画、oa-codeのROSワークスペース接続を確認。

MuJoCoの柔らかい関節限界により発生する最大0.0001rad/m以内の境界超過のみROS状態値で正規化します。
物理状態は変更せず、元の値は `/openarm/sim_state` に残します。
2.0の受動指はURDF mimicとして扱い、ros2_control指令インターフェースには含めません。

追加確認: GUIの右手デモを最後まで実行し、MoveIt経由の左右グリッパー開閉も成功しました。
変換前後の関節軸・可動域・質量・慣性、および10通りのランダム姿勢で全公式ボディの位置・向きが一致することを確認しました。
OMPLで得た経路が時間補間後の衝突検証で棄却された場合は、最大5回まで再計画します。棄却された経路は実行しません。
